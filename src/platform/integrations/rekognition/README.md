# rekognition

AWS Rekognition CompareFaces adapter for pre-disbursement facial verification
(both TRANSFER and CHEQUE_PICKUP channels — the channel doesn't change the
comparison logic, only contextual fields on the caller's record). Built in
Phase 8 (loans module). See PHASE_8_NOTES.md.
